{
  "identifier": "default",
  "description": "Default capability set for NurChat",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "notification:default"
  ]
}
